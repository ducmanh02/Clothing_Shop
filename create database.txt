-- Xóa bảng Feedback
DROP TABLE IF EXISTS Feedback;

-- Xóa bảng Order_Items
DROP TABLE IF EXISTS Order_Items;

-- Xóa bảng Orders
DROP TABLE IF EXISTS Orders;

-- Xóa bảng Cart_Items
DROP TABLE IF EXISTS Cart_Items;

-- Xóa bảng Cart
DROP TABLE IF EXISTS Cart;

-- Xóa bảng Products
DROP TABLE IF EXISTS Products;

-- Xóa bảng Categories
DROP TABLE IF EXISTS Categories;

-- Xóa bảng Brands
DROP TABLE IF EXISTS Brands;

-- Xóa bảng Users
DROP TABLE IF EXISTS Users;

-- Tạo lại bảng Users với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Users (
    user_id VARCHAR(50) PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    full_name VARCHAR(100),
    address VARCHAR(255),
    phone VARCHAR(15),
    is_admin BOOLEAN DEFAULT FALSE
);

-- Tạo lại bảng Brands với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Brands (
    brand_id VARCHAR(50) PRIMARY KEY,
    brand_name VARCHAR(50) UNIQUE NOT NULL
);

-- Tạo lại bảng Categories với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Categories (
    category_id VARCHAR(50) PRIMARY KEY,
    category_name VARCHAR(50) UNIQUE NOT NULL
);

-- Tạo lại bảng Products với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Products (
    product_id VARCHAR(50) PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL,
    stock_quantity INT NOT NULL,
    brand_id VARCHAR(50),
    category_id VARCHAR(50),
    image_url VARCHAR(255),
    FOREIGN KEY (brand_id) REFERENCES Brands(brand_id),
    FOREIGN KEY (category_id) REFERENCES Categories(category_id)
);

-- Tạo lại bảng Cart với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Cart (
    cart_id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(50) DEFAULT 'Active',
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
);

-- Tạo lại bảng Cart_Items với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Cart_Items (
    cart_item_id VARCHAR(50) PRIMARY KEY,
    cart_id VARCHAR(50),
    product_id VARCHAR(50),
    quantity INT,
    FOREIGN KEY (cart_id) REFERENCES Cart(cart_id),
    FOREIGN KEY (product_id) REFERENCES Products(product_id)
);

-- Tạo lại bảng Orders với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Orders (
    order_id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50),
    total_amount DECIMAL(10, 2) NOT NULL,
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(50) DEFAULT 'Pending',
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
);

-- Tạo lại bảng Order_Items với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Order_Items (
    order_item_id VARCHAR(50) PRIMARY KEY,
    order_id VARCHAR(50),
    product_id VARCHAR(50),
    quantity INT,
    unit_price DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (order_id) REFERENCES Orders(order_id),
    FOREIGN KEY (product_id) REFERENCES Products(product_id)
);

-- Tạo lại bảng Feedback với ID là VARCHAR(50) và không tự tăng
CREATE TABLE Feedback (
    feedback_id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50),
    product_id VARCHAR(50),
    rating INT,
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES Users(user_id),
    FOREIGN KEY (product_id) REFERENCES Products(product_id)
);

-- Bảng Sản phẩm
ALTER TABLE Products
ADD COLUMN size VARCHAR(20);



ALTER TABLE Feedback MODIFY COLUMN feedback_id VARCHAR(50) PRIMARY KEY;
